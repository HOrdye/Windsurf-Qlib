# 项目基础规范
1. **架构约束**  
   - 强制使用 `/data/qlib` 目录作为统一数据仓库路径  
   - 所有量化模型必须继承 `BaseModel` 类并实现 `train/predict` 接口  
   - 禁止修改 `/core/kernel` 目录下的引擎核心模块  

2. **开发准则**  
   ```xml
   <coding_guidelines>
   - 使用 TypeScript 实现策略逻辑层,Python 仅用于数据预处理
   - 金融时间序列处理必须使用 qlib.data.Dataset 封装
   - 实时回测引擎必须通过 Rust FFI 调用
   </coding_guidelines>

   <windows_specific>
   - DPI 缩放适配使用注册表 HKEY_CURRENT_USER\Control Panel\Desktop 值
   - 文件操作必须调用 NativeBridge 的 win32-file-io 模块
   - 内存管理遵循 2GB 堆上限规则
   </windows_specific>
   量化业务规则
3.数据管理

原始数据文件仅允许存放在 /data/raw 目录
特征工程输出必须生成特征版本快照    (格式:YYYYMMDD_HHMMSS.feather)
回测结果数据需自动压缩为 zstd 格式存储

4.量化业务规则
- 模型训练前必须执行数据泄露检查
- GPU 加速仅限于 batch_size > 256 的场景
- 特征重要性低于 0.01 的参数需自动剔除

5.其他
- 禁止修改 qlib 的核心库
- 禁止修改 qlib 的数据仓库
- 禁止修改 qlib 的配置文件      
    <testing_policy>
    - 回测结果必须通过 JB 正态性检验 (p < 0.05)
    - 内存泄漏检测间隔 ≤ 30 分钟
     </testing_policy>
     # WebSocket 通信规范
     
     ## 1. 类型定义规范
     - 所有 WebSocket 相关接口必须使用严格类型定义
     - 禁止使用 NodeJS 特定类型（如 NodeJS.Timeout），应使用浏览器兼容类型（如 number）
     - 所有消息类型必须使用枚举或联合类型定义，禁止使用字符串字面量
     
     ## 2. 连接管理规范
     - 必须实现指数退避重连机制，最小间隔 1 秒，最大间隔 30 秒
     - 必须实现心跳检测，默认间隔 30 秒
     - 必须提供连接状态变化的事件通知机制
     - 连接失败超过 10 次后必须停止自动重连并通知用户
     
     ## 3. 消息处理规范
     - 必须实现消息队列机制，队列上限为 100 条消息
     - 请求-响应模式必须支持超时处理，默认超时时间 10 秒
     - 所有消息必须包含：类型、负载、时间戳和唯一ID
     
     ## 4. 错误处理规范
     - 所有 WebSocket 错误必须被捕获并记录到日志
     - 用户可见的错误提示必须友好且提供解决建议
     - 必须实现错误恢复机制，包括自动重连和会话恢复
     
     ## 5. 资源管理规范
     - 组件卸载时必须清理所有 WebSocket 资源和定时器
     - 必须使用单例模式管理 WebSocket 连接
     - 长时间未活动的连接（超过 5 分钟）应自动关闭
     
     ## 6. 配置管理规范
     - 禁止硬编码端口号和服务地址，必须使用配置文件或环境变量
     - 服务启动前必须检查端口占用情况
     - 必须支持不同环境（开发、测试、生产）的配置切换
     
     ## 7. React 组件规范
     - 禁止使用 React.FC 类型定义方式
     - 必须使用函数组件并显式定义 Props 接口
     - 必须确保类型定义与 React 18+ 兼容
     
     ## 8. 状态管理规范
     - 必须使用正确的 Zustand 类型定义方式
     - 必须为状态管理库提供明确的类型参数
     - 禁止使用类型断言(as),除非有明确的技术限制
     
     ## 9. 测试要求
     - 必须提供专门的 WebSocket 测试页面
     - 必须实现模拟服务器用于测试
     - 必须提供连接状态和消息流的可视化
     - 测试覆盖率要求 :WebSocket 服务核心功能 ≥ 90%
     - 在 PowerShell 中使用分号而不是 && 来连接命令